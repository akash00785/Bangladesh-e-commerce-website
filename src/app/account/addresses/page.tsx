import AddressesPage from "@/components/account/AddressesPage";

export const metadata = {
  title: "সেভ করা ঠিকানা — Fashion Bazar",
};

export default function AddressesRoute() {
  return <AddressesPage />;
}
